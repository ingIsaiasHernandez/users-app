import React from 'react';

const UsersList = ({userslist}) => {
    return (
        <div>
            {/* {userslist.map(user =>(
                <li>user.name</li>
            ))} */}
            
        </div>
    );
};

export default UsersList;